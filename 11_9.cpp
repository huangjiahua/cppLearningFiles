#include <vector>
#include <map>
#include <string>
#include <list>

using namespace std;

int main()
{
    map<string, list<int> > msl;

    map<vector<int>::iterator, int> mv;
    map<list<int>::iterator, int> ml; 
}
